from setuptools import setup, find_packages

setup(
    name='booklover',
    version='0.1',
    packages=find_packages(),
    description='A small library for managing a book collection',
    author='Your Name',
    install_requires=[],
)

